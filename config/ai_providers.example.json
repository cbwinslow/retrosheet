{
  "providers": {
    "openrouter": {
      "base_url": "https://openrouter.ai/api/v1",
      "api_key_env": "OPENROUTER_API_KEY",
      "default_model_env": "OPENROUTER_MODEL"
    },
    "groq": {
      "base_url": "https://api.groq.com/openai/v1",
      "api_key_env": "GROQ_API_KEY",
      "default_model_env": "GROQ_MODEL"
    },
    "codex": {
      "provider": "openai",
      "purpose": "coding, data-quality agents, SQL generation review, and orchestration"
    }
  },
  "rule": "AI providers may plan, explain, summarize, and call approved tools. They should not invent model probabilities without reading warehouse/model outputs."
}
